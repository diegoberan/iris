[project]
name = "iris-site"
version = "0.1.0"
description = "Iris Labs marketing + signup site"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2",
    "python-dotenv>=1.0.0",
    "stripe>=11.0.0",
    "gunicorn>=23.0.0",
]
